# 验证记录

日期：2026-09-08。交付形态：本地开发版源码与 ZIP，无第三方运行依赖。

## v1.0.1 修复验证

用户截图确认 v1.0.0 的 `devAllowedDomains` IP 地址被 Figma manifest 校验拒绝。v1.0.1 改为官方文档列出的 localhost 形式，并同步修改 UI 请求地址与服务端 Host 白名单，仍仅绑定本机回环网卡。

修复后 10 项测试全部通过；鉴权测试增加 localhost 实际 HTTP 连接断言，非法 Host 仍被拒绝。Edge sandbox iframe 的实际 localhost 配对、轮询、结果回传及断开测试通过。Figma 主线程为模拟；修正版尚未由用户在真实 Figma 中确认成功加载。

## 已验证

- `node --test tests/*.test.mjs`：10 项通过，0 失败。
- 本地 HTTP 服务的控制端/插件端令牌隔离、来源和 Host 检查。
- 多会话隔离、相同请求去重、结果重复上传不覆盖原结果。
- 断开时取消尚未执行的操作、排队过期后不执行。
- 实际 Node 子进程通过 stdio 完成 MCP 初始化、工具枚举、状态读取，以及本地 HTTP 的命令/结果往返。Figma 接收端在此测试中是模拟客户端。
- Figma API 模拟测试：可编辑文字、字体加载、层级创建、失败时新节点清理、非法属性拒绝、页面删除保护和节点读取上限。
- JavaScript 语法与打包插件清单校验。
- 插件专用 Skill 校验通过。
- Microsoft Edge 无头浏览器的 sandbox iframe 测试：配对、轮询、本地服务回传、断开均成功，无页面脚本错误。Figma 主线程仍为模拟。`plugin-preview.png` 是此测试中的界面截图。
- 已执行 `Install-Codex.ps1`，`codex mcp get figma-local-bridge` 确认 enabled / stdio，入口指向本包 `server/mcp.mjs`；未验证当前任务热加载，新任务中再联调。

## 尚未验证

- 在真实 Figma 桌面应用导入 manifest 和运行插件。
- 实际客户端的本地网络/CSP 行为、字体可用性、渲染效果和撤销行为。
- 真实文件修改与云端保存。
- Codex 桌面新任务实际加载本 MCP 后的用户侧联调。

因此“测试通过”仅描述上述本地与模拟测试，不表示已经创建或修改了用户的云端 Figma 文件。

## 实机验收步骤

1. 在 Figma 桌面创建空白文件，导入并运行插件。
2. 启动桥接，输入配对码，确认显示当前文件名。
3. 在已加载本地 MCP 的 Codex 新任务中调用状态、读取文档。
4. 执行 `examples/card.json` 的 operations，确认画布出现一张卡片、文字可编辑。
5. 修改标题的 characters，并导出 PNG 检查结果。
6. 执行 Figma 撤销，确认上一批修改回退；关闭插件后确认无法继续向该会话提交新操作。
7. 等待 Figma 保存完成，重新打开文件核对内容。

## v1.0.2 实机排查

真实 MCP status 返回会话在线，但名字为 Untitled、页面为空。一次 document 只读任务 33055985-e71d-4757-b437-4522ac6e9e91 等待后仍为 running，尚未收到文档结果。本次没有发送任何写入操作。

修订 UI 消息接收：不再依赖 event.source === parent，以随机握手 nonce 和任务 ID 匹配消息；连接按钮等待文件信息到达，握手自动重试，执行等待增加超时提示。主线程在 showUI 前注册消息处理器。10 项测试通过，浏览器测试验证正常来源、空来源消息及错误 nonce 拒绝，配对/执行/断开通过。真实 Figma 需重新运行新版插件后再验证，不能将服务在线当作读写成功。

## 真实 Figma 联调通过（2026-09-08）

会话 62afa7dc-df69-4141-bdf6-86f518796ad3，文件 Fix It! 城市补丁 · iPhone 17 Pro UI 严格同步版。
真实 MCP 读取成功，Page 1 原有 24 个 Frame。创建临时 Frame 170:2 及 TEXT 170:3，修改文字为 Bridge OK、字号为 24 并修改填色，回读确认值生效；PNG 导出成功并目视检查。随后删除临时 Frame，回读确认画板恢复为原有 24 个，临时节点不再存在，选区仍为空。测试没有修改原有设计节点。已验证当前文件内的读取、创建、修改、字体加载、PNG 导出、删除；未验证所有属性、顶层文件创建或重新打开后的云端持久化。

## v1.0.3 iPhone 17 Pro 安全区适配

真实 Figma 文件 `Fix It! 城市补丁 · iPhone 17 Pro UI 严格同步版` 的 24 个顶层界面已统一为 402×874 pt，顶部安全区 62 pt、底部安全区 34 pt。正文区域位于 y=62 至 y=840；首页类底栏位于正文区域底部。长详情页保留内部纵向滚动，弹窗内容限制在安全区内，Toast 状态页恢复完整首屏显示。

逐页结构回读确认 24/24 个界面满足宽高和安全区条件，并逐页导出 PNG 检查首页、详情、修复前后、五步上报、搜索/筛选、通知、Toast 与空状态。原有业务文字、图片与画板顺序未删除。调整前完整布局树备份保存在交付目录上一级的 `safe-area-layout-backup.json`。
